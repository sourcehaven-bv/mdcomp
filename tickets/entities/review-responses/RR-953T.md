---
finding: Every call to run_sql creates a new SQLAlchemy engine, which maintains connection pools. Creating a new engine per query means connection pool per call, pools never properly disposed, and under load will exhaust database connections.
id: RR-953T
resolution: Added engine caching via _engine_cache dict. Engines are now reused for the same db_url, preventing connection pool exhaustion. Added test_engine_caching test to verify.
severity: critical
status: addressed
title: Connection pool exhaustion
type: review-response
---
